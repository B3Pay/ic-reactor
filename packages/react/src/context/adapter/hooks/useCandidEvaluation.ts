import { AdapterHooks } from ".."

/**
 * Accesses the `CandidEvaluation` function to evaluate the actor's Candid interface from a string
 * into an `idlFactory`.
 */
export const useCandidEvaluation = AdapterHooks.useCandidEvaluation
