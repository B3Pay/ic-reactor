{
  "name": "@ic-reactor/shared",
  "version": "0.1.0",
  "description": "Shared utilities and types for the ic-reactor monorepo.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run tsc -b",
    "test": "bun test"
  }
}
