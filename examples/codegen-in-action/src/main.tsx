import { StrictMode, useEffect, useState } from "react"
import { createRoot } from "react-dom/client"
import { QueryClientProvider } from "@tanstack/react-query"
import { queryClient } from "./lib/client"

// Generated by the Vite plugin: the reactor's bound hooks, and with
// `factories: true` a query or mutation object per method of the .did.
import {
  addMutation,
  getQuery,
  incMutation,
  useBackendMethod,
} from "./canisters-vite/backend"

import "./style.css"
import { getCanisterEnv } from "@icp-sdk/core/agent/canister-env"

function App() {
  return (
    <main>
      <h1>IC Reactor Codegen in Action</h1>
      <p>
        Run <code>npm run codegen:vite</code> and{" "}
        <code>npm run codegen:cli</code>
        to compare generated outputs.
      </p>
      <ul>
        <li>
          Vite plugin output: <code>src/canisters-vite/backend</code>
        </li>
        <li>
          CLI output: <code>src/canisters-cli/backend</code>
        </li>
      </ul>

      <CookieSection />
    </main>
  )
}

function CookieSection() {
  const [cookieString, setCookieString] = useState(
    () => document.cookie || "(no cookies)"
  )
  const icEnvRaw = cookieString
    .split("; ")
    .find((c) => c.startsWith("ic_env="))
    ?.split("=")[1]
  const icEnv = icEnvRaw ? decodeURIComponent(icEnvRaw) : "(not set)"

  // Generated factories: `get` is a query method, `inc` and `add` are updates.
  const { data: counter } = getQuery.useQuery()

  const { mutate: inc } = incMutation.useMutation({
    invalidateQueries: [getQuery.getQueryKey()],
  })

  // The bound hooks work next to them: the unified method hook for `add`.
  const { call: add } = useBackendMethod({
    functionName: "add",
    invalidateQueries: [getQuery.getQueryKey()],
  })

  useEffect(() => {
    const onFocus = () => setCookieString(document.cookie || "(no cookies)")
    window.addEventListener("focus", onFocus)
    return () => window.removeEventListener("focus", onFocus)
  }, [])

  return (
    <section style={{ marginTop: 24 }}>
      <h2>🔎 Dev: Cookies & Reactor</h2>
      <div style={{ marginBottom: 8 }}>
        <strong>document.cookie:</strong>
        <pre style={{ whiteSpace: "pre-wrap", margin: "8px 0" }}>
          {cookieString}
        </pre>
      </div>
      {JSON.stringify(
        getCanisterEnv({
          cookieName: "ic_env",
        })
      )}
      <div style={{ marginBottom: 8 }}>
        <strong>Decoded ic_env:</strong>
        <pre style={{ whiteSpace: "pre-wrap", margin: "8px 0" }}>{icEnv}</pre>
      </div>

      <div style={{ display: "flex", gap: 16, alignItems: "center" }}>
        <div>
          <strong>counter (get):</strong>
          <div style={{ fontSize: 20, marginTop: 6 }}>
            {counter ? String(counter) : "(no response)"}
          </div>
        </div>

        <div>
          <strong>inc (incMutation.useMutation):</strong>
          <div>
            <button onClick={() => inc([])} className="btn">
              Increment
            </button>
          </div>
        </div>

        <div>
          <strong>add (useBackendMethod.call):</strong>
          <div>
            <button
              onClick={async () => {
                await add(["5"])
                setCookieString(document.cookie || "(no cookies)")
              }}
              className="btn"
            >
              Add 5
            </button>
          </div>
        </div>

        <div>
          <strong>add (addMutation.execute):</strong>
          <div>
            {/* The imperative call, as a loader or a script outside React makes it. */}
            <button
              onClick={async () => {
                await addMutation.execute(["1"])
                await getQuery.invalidate()
              }}
              className="btn"
            >
              Add 1
            </button>
          </div>
        </div>
      </div>
    </section>
  )
}

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <QueryClientProvider client={queryClient}>
      <App />
    </QueryClientProvider>
  </StrictMode>
)
