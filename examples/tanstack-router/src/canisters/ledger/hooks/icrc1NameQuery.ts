/**
 * Query Hook: icrc1_name
 *
 * Auto-generated by @ic-reactor/cli
 * This hook wraps the icrc1_name query method.
 *
 * @example
 * // Use the hook
 * const { data, isLoading } = useIcrc1NameQuery()
 *
 * // Or access the query object directly
 * const data = await icrc1NameQuery.fetch()
 * icrc1NameQuery.invalidate()
 */

import { createQuery } from "@ic-reactor/react"
import { ledgerReactor } from "../reactor"

// ═══════════════════════════════════════════════════════════════════════════
// QUERY INSTANCE
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Query for icrc1_name
 *
 * Provides:
 * - .useQuery() - React hook
 * - .fetch() - Direct fetch (for loaders)
 * - .invalidate() - Invalidate cache
 * - .getQueryKey() - Get query key
 * - .getCacheData() - Read from cache
 */
export const icrc1NameQuery = createQuery(ledgerReactor, {
  functionName: "icrc1_name",
  // Customize your query options:
  // staleTime: 5 * 60 * 1000,
  // select: (data) => data,
})

// ═══════════════════════════════════════════════════════════════════════════
// CONVENIENCE EXPORTS
// ═══════════════════════════════════════════════════════════════════════════

/**
 * React hook for icrc1_name
 */
export const useIcrc1NameQuery = icrc1NameQuery.useQuery

/**
 * Fetch icrc1_name directly (for loaders, server components, etc.)
 */
export const fetchIcrc1Name = icrc1NameQuery.fetch

/**
 * Invalidate icrc1_name query cache
 */
export const invalidateIcrc1Name = icrc1NameQuery.invalidate

/**
 * Get icrc1_name query key (for cache manipulation)
 */
export const getIcrc1NameQueryKey = icrc1NameQuery.getQueryKey

/**
 * Get cached data for icrc1_name
 */
export const getIcrc1NameCacheData = icrc1NameQuery.getCacheData
