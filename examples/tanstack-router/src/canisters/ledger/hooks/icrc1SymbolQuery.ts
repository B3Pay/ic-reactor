/**
 * Query Hook: icrc1_symbol
 *
 * Auto-generated by @ic-reactor/cli
 * This hook wraps the icrc1_symbol query method.
 *
 * @example
 * // Use the hook
 * const { data, isLoading } = useIcrc1SymbolQuery()
 *
 * // Or access the query object directly
 * const data = await icrc1SymbolQuery.fetch()
 * icrc1SymbolQuery.invalidate()
 */

import { createQuery } from "@ic-reactor/react"
import { ledgerReactor } from "../reactor"

// ═══════════════════════════════════════════════════════════════════════════
// QUERY INSTANCE
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Query for icrc1_symbol
 *
 * Provides:
 * - .useQuery() - React hook
 * - .fetch() - Direct fetch (for loaders)
 * - .invalidate() - Invalidate cache
 * - .getQueryKey() - Get query key
 * - .getCacheData() - Read from cache
 */
export const icrc1SymbolQuery = createQuery(ledgerReactor, {
  functionName: "icrc1_symbol",
  // Customize your query options:
  // staleTime: 5 * 60 * 1000,
  // select: (data) => data,
})

// ═══════════════════════════════════════════════════════════════════════════
// CONVENIENCE EXPORTS
// ═══════════════════════════════════════════════════════════════════════════

/**
 * React hook for icrc1_symbol
 */
export const useIcrc1SymbolQuery = icrc1SymbolQuery.useQuery

/**
 * Fetch icrc1_symbol directly (for loaders, server components, etc.)
 */
export const fetchIcrc1Symbol = icrc1SymbolQuery.fetch

/**
 * Invalidate icrc1_symbol query cache
 */
export const invalidateIcrc1Symbol = icrc1SymbolQuery.invalidate

/**
 * Get icrc1_symbol query key (for cache manipulation)
 */
export const getIcrc1SymbolQueryKey = icrc1SymbolQuery.getQueryKey

/**
 * Get cached data for icrc1_symbol
 */
export const getIcrc1SymbolCacheData = icrc1SymbolQuery.getCacheData
