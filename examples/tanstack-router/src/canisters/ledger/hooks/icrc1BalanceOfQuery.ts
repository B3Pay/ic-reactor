/**
 * Query Factory: icrc1_balance_of
 *
 * Auto-generated by @ic-reactor/cli
 *
 * @example
 * // Use in components
 * const { data, isLoading } = icrc1BalanceOfQuery([arg1, arg2]).useQuery()
 *
 * // Prefetch in loaders
 * const data = await icrc1BalanceOfQuery([arg1, arg2]).fetch()
 *
 * // Invalidate cache
 * icrc1BalanceOfQuery([arg1, arg2]).invalidate()
 *
 * // Get query key for cache manipulation
 * const key = icrc1BalanceOfQuery([arg1, arg2]).getQueryKey()
 */

import { createQueryFactory } from "@ic-reactor/react"
import { ledgerReactor } from "../reactor"

/**
 * Query factory for icrc1_balance_of
 *
 * Creates a query instance with the provided arguments.
 * Each unique set of args gets its own cached query.
 */
export const icrc1BalanceOfQuery = createQueryFactory(ledgerReactor, {
  functionName: "icrc1_balance_of",
  refetchInterval: 5 * 1000,
})
