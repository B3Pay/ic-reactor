/**
 * AUTO-GENERATED BY @ic-reactor/vite-plugin
 *
 * Canister: counter
 * Generated: 2026-01-09T11:50:07.230Z
 *
 * This file provides advanced type-safe React hooks for interacting with the
 * counter canister using ic-reactor.
 */

import {
  DisplayReactor,
  createActorHooks,
  type ReactorParameters,
} from "@ic-reactor/react"

// ═══════════════════════════════════════════════════════════════════════════
// USER-PROVIDED CLIENT MANAGER
// ═══════════════════════════════════════════════════════════════════════════
import { clientManager } from "../../lib/client"

// Import generated declarations from @icp-sdk/bindgen
import { idlFactory, type _SERVICE } from "./declarations/counter.did"

// ═══════════════════════════════════════════════════════════════════════════
// TYPES
// ═══════════════════════════════════════════════════════════════════════════

export type CounterReactor = DisplayReactor<_SERVICE>

export type CreateCounterReactorOptions = Partial<ReactorParameters>

// ═══════════════════════════════════════════════════════════════════════════
// FACTORIES
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Creates a DisplayReactor instance for the counter canister.
 *
 * @param options - partial ReactorParameters to override defaults
 * @returns a new DisplayReactor instance
 */
export function createCounterReactor(
  options: CreateCounterReactorOptions = {}
): CounterReactor {
  return new DisplayReactor<_SERVICE>({
    clientManager,
    idlFactory,
    name: "counter",
    ...options,
  })
}

/**
 * Creates a full set of React hooks for a DisplayReactor instance.
 *
 * @param reactor - the reactor instance to generate hooks for
 * @returns query, mutation, and subscription hooks
 */
export function createCounterHooks(reactor: CounterReactor) {
  return createActorHooks(reactor)
}

// ═══════════════════════════════════════════════════════════════════════════
// DEFAULT INSTANCE & HOOKS
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Default DisplayReactor instance with standard configuration.
 */
export const counterReactor = createCounterReactor()

/**
 * Pre-generated hooks using the default counterReactor instance.
 * These are ready to use in your React components.
 */
const defaultHooks = createCounterHooks(counterReactor)

export const {
  useActorQuery: useCounterQuery,
  useActorSuspenseQuery: useCounterSuspenseQuery,
  useActorInfiniteQuery: useCounterInfiniteQuery,
  useActorSuspenseInfiniteQuery: useCounterSuspenseInfiniteQuery,
  useActorMutation: useCounterMutation,
  useActorMethod: useCounterMethod,
} = defaultHooks

// ═══════════════════════════════════════════════════════════════════════════
// RE-EXPORTS
// ═══════════════════════════════════════════════════════════════════════════

export { idlFactory }
export type { _SERVICE as CounterService }
