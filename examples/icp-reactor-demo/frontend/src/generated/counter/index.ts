/**
 * AUTO-GENERATED BY @ic-reactor/vite-plugin
 *
 * Canister: counter
 * Generated: 2026-01-09T12:43:15.932Z
 *
 * This file provides type-safe React hooks for interacting with the
 * counter canister using ic-reactor.
 */

import { DisplayReactor, createActorHooks } from "@ic-reactor/react"

// ═══════════════════════════════════════════════════════════════════════════
// USER-PROVIDED CLIENT MANAGER
// ═══════════════════════════════════════════════════════════════════════════
// The clientManager is imported from the user's own configuration file.
// This allows full customization of agent options, network settings, etc.
import { clientManager } from "../../lib/client"

// Import generated declarations from @icp-sdk/bindgen
import { idlFactory, type _SERVICE } from "./declarations/counter.did"

// ═══════════════════════════════════════════════════════════════════════════
// REACTOR INSTANCE
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Counter Reactor with Display type transformations.
 * Automatically converts bigint → string, Principal → string, etc.
 */
export const counterReactor = new DisplayReactor<_SERVICE>({
  clientManager,
  idlFactory,
  name: "counter",
})

// ═══════════════════════════════════════════════════════════════════════════
// HOOKS
// ═══════════════════════════════════════════════════════════════════════════

/**
 * React hooks for the counter canister.
 */
const {
  useActorQuery: useCounterQuery,
  useActorSuspenseQuery: useCounterSuspenseQuery,
  useActorInfiniteQuery: useCounterInfiniteQuery,
  useActorSuspenseInfiniteQuery: useCounterSuspenseInfiniteQuery,
  useActorMutation: useCounterMutation,
  useActorMethod: useCounterMethod,
} = createActorHooks(counterReactor)

export {
  useCounterQuery,
  useCounterSuspenseQuery,
  useCounterInfiniteQuery,
  useCounterSuspenseInfiniteQuery,
  useCounterMutation,
  useCounterMethod,
}

// ═══════════════════════════════════════════════════════════════════════════
// RE-EXPORTS
// ═══════════════════════════════════════════════════════════════════════════

export { idlFactory }
export type { _SERVICE as CounterService }
