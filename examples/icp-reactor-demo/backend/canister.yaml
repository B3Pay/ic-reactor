# yaml-language-server: $schema=https://github.com/dfinity/icp-cli/raw/refs/tags/v0.1.0-beta.2/docs/schemas/canister-yaml-schema.json

name: backend

build:
  steps:
    # For this demo, we use a pre-built Hello World canister
    # In production, you would build from Rust/Motoko source
    - type: pre-built
      path: dist/backend.wasm
      sha256: 17a05e36278cd04c7ae6d3d3226c136267b9df7525a0657521405e22ec96be7a
