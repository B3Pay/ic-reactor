---
title: UnwrapOkErrResult
editUrl: false
next: true
prev: true
---

> **UnwrapOkErrResult**\<`T`\> = `T` *extends* `object` ? `U` : `T` *extends* `object` ? `U` : `T` *extends* `object` ? `E` : `T` *extends* `object` ? `E` : `T`

Defined in: [types/result.ts:1](https://github.com/B3Pay/ic-reactor/blob/d7917e29daff163fdeec6d2e30eaec8ba1450c86/packages/core/src/types/result.ts#L1)

## Type Parameters

### T

`T`
