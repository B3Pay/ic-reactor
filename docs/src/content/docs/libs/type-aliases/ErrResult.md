---
title: ErrResult
editUrl: false
next: true
prev: true
---

> **ErrResult**\<`T`\> = `T` *extends* `object` ? `never` : `T` *extends* `object` ? `never` : `T` *extends* `object` ? `E` : `T` *extends* `object` ? `E` : `never`

Defined in: [types/result.ts:29](https://github.com/B3Pay/ic-reactor/blob/e0f3465befa2e08ee6f1f6ff7d4b00fcc6d1ff89/packages/core/src/types/result.ts#L29)

## Type Parameters

### T

`T`
