---
title: ErrResult
editUrl: false
next: true
prev: true
---

> **ErrResult**\<`T`\> = `T` *extends* `object` ? `never` : `T` *extends* `object` ? `never` : `T` *extends* `object` ? `E` : `T` *extends* `object` ? `E` : `never`

Defined in: [types/result.ts:29](https://github.com/B3Pay/ic-reactor/blob/d7917e29daff163fdeec6d2e30eaec8ba1450c86/packages/core/src/types/result.ts#L29)

## Type Parameters

### T

`T`
